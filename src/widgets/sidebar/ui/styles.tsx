import styled from 'styled-components';
import { SIDEBAR_WIDTH } from '../model';

export const Nav = styled.nav`
  width: ${SIDEBAR_WIDTH}px;
  height: 100%;
`;

export const Ul = styled.ul`
  width: 100%;
  padding: 0.5rem 1rem;
`;

export const Li = styled.li`
  margin-bottom: 4px;
`;
